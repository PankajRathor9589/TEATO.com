# TEATO React app - copy to .env
# API base URL (leave empty when using dev proxy)
REACT_APP_API_URL=http://localhost:5000

# Socket.io uses same origin by default; set if API is on different host
# REACT_APP_SOCKET_URL=http://localhost:5000
